{
  "name": "infected-zone-top-down-shooter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:pages": "vite build --base=/2.5D-Game/",
    "build:site": "npm run build:pages && npm --prefix presentation run export -- --output ../dist/presentation",
    "preview": "vite preview --host 127.0.0.1",
    "preview:pages": "vite preview --host 127.0.0.1 --base=/2.5D-Game/"
  },
  "dependencies": {
    "three": "^0.184.0",
    "vite": "^8.0.16"
  }
}
